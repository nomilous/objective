


var repl = require('repl');
var running, r;

// isaTTY

module.exports.start = function(config) {

  // TODO: local repl instead of prompt

  // if (running) return;

  // running = true;

  // r = repl.start({
  //   prompt: config.title + '> ',
  //   input: process.stdin,
  //   output: process.stdout,
  //   terminal: true,
  //   useGlobal: true
  // });

}

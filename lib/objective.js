// Generated by CoffeeScript 1.9.2
var fs, program;

fs = require('fs');

program = require('commander');

program.version(JSON.parse(fs.readFileSync(__dirname + '/../package.json')).version).option('-R, --register', 'Register new user.').option('-r, --reset', 'Re-register as existing user by email (forgot password).').option('-C, --create', 'Create new objective.').option('-S, --create-spec', 'Create new spec objective.').option('-T, --template [name]', 'Create new objective from template').option('-f, --file [file]', 'Specify objective file namepart.').option('-F, --force', 'Force action.').option('-j, --js', 'Use javascript for --create.').parse(process.argv);

if (program.register) {
  return require('./actions/register')["do"](program, false, function() {});
}

if (program.reset) {
  return require('./actions/register')["do"](program, true, function() {});
}

program.template || (program.template = 'default');

if (program.create) {
  return require('./actions/create')["do"](program, program.template, function() {});
}

if (program.createSpec) {
  return require('./actions/create')["do"](program, 'spec', function() {});
}

require('./actions/run')["do"](program, function() {});

// Generated by CoffeeScript 1.9.2
var fs, program;

fs = require('fs');

program = require('commander');

program.version(JSON.parse(fs.readFileSync(__dirname + '/../package.json')).version).option('-R, --register', 'Register new user.').parse(process.argv);

if (program.register) {
  return require('./actions/register')["do"]();
}

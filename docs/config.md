### objective.config.aliases

```javascript

objective( 'make cookies', {
  aliases: {
    'eo': 'socket.io',
    'thing': {
      be: function() {}
    },
    'Or' : require('||')
  }
}, function(eo, thing, Or) {
  
  thing.be(new or(eo));

})

```
